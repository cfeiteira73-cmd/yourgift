{
  "framework": "nextjs",
  "buildCommand": "pnpm --filter @yourgift/shared run build && pnpm --filter web run build",
  "outputDirectory": "apps/web/.next",
  "installCommand": "pnpm install --no-frozen-lockfile"
}
